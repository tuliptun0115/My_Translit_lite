# My_Translit_lite - .cursorrules

# 專案規範與開發指引

## 1. 程式碼風格 (Code Style)
*   **語言**：繁體中文註釋，介面文字必須為繁體中文。
*   **框架**：React + TypeScript + Tailwind CSS。
*   **排版**：使用 Prettier 標準規範。
*   **命名**：
    *   組件：PascalCase (e.g., `TranslitCard.tsx`)。
    *   變數/函數：camelCase (e.g., `handleTranslate`)。
    *   樣式：Tailwind utility-first。

## 2. Tailwind 擴充規範 (Design Tokens)
*   必須使用 `Pink 50-600` 色系。
*   主要圓角：`rounded-2xl` (1rem)。
*   主要陰影：`shadow-xl`。

## 3. 技術開發限制 (Technical Constraints)
*   **API 使用**：嚴禁在前端 Hardcode API Key，必須使用 `VITE_GEMINI_API_KEY`。
*   **防禦性編程**：重要 API 調用必須包裹 `try-catch` 塊。
*   **TTS 優化**：必須在互動行為（Click）中觸發 `window.speechSynthesis.speak` 初始調用。

## 4. 錯誤訊息規範
*   發生錯誤時，提供溫暖且直觀的提示（如：「哎呀！翻譯出了一點小問題，請再試一次吧 🌸」）。
*   401 錯誤：明確提示「授權已過期，請重新整理頁面」。

## 5. 文件維護
*   修改核心邏輯後，必須同步更新 `CHANGELOG.md` 與 `TECH_DOC.md`。
