# 好好唸 - 專案開發規章 (.cursorrules)

# 🚀 全能技術夥伴開發手則 (中文版)

## 1. 程式碼風格與語系規範
*   **介面語言**：所有使用者看見的文字、按鈕、提示、文件紀錄一律使用**繁體中文**。
*   **註釋風格**：複雜邏輯必須附帶繁體中文註釋。
*   **技術棧**：React (latest) + TypeScript + Tailwind CSS (v4+)。

## 2. 介面與設計語彙 (Design Tokens)
*   **色彩主切面**：必須使用專案定義的 `Pink` (粉紅) 與 `Gray` (深灰) 色系。
*   **組件形體**：
    *   圓角：預設採用 `rounded-2xl` (1rem)。
    *   陰影：提升對比時使用 `shadow-xl`。
*   **動畫**：過渡動畫應採用 `framer-motion`，以「絲滑、溫暖」為主要感受。

## 3. 技術開發軍規 (Engineering Excellence)
*   **防禦性編程**：所有的 API 調用（Gemini, Web Speech）必須實作 `try-catch` 與友善的 Fallback 機制。
*   **行動端優先 (Mobile First)**：
    *   優先確保留 iPhone Safari 的顯示與功能穩定。
    *   所有的 `localStorage` 操作必須使用安全工具類，防止 `SecurityError` 導致畫面空白。
*   **效能與 Token 節流**：
    *   僅在必要時發送 AI 請求。
    *   單次結果後的語音重播應直接調用本地緩存。

## 4. 錯誤訊息規範 (Error Handling)
*   **PM 友善化**：禁止直接顯示原始的 `Stack Trace` 或 `TypeError` 給使用者。
*   **具体指引**：例如當 `Failed to fetch` 發生時，應提示使用者檢查網路環境或是否需要開啟 VPN。

## 5. 文件資產維護 (Asset Management)
*   每當完成核心邏輯修改或 UI 重構後，必須同步更新「技術資產五件套」：
    1. `.cursorrules` (開發規章)
    2. `PRD.md` (產品需求)
    3. `TECH_DOC.md` (技術說明)
    4. `UI_UX_GUIDE.md` (設計規範)
    5. `CHANGELOG.md` (變更日誌)

---
*Generated by Antigravity Technical Partner.*
